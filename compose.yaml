services:
  lcn2mqtt:
    build: .
    image: lcn2mqtt:latest
    container_name: lcn2mqtt
    restart: unless-stopped
    env_file:
      - path: ./stack.env
        required: false
      - path: ./.env
        required: false
